@model List<TodoDTO>
@{
    Layout = null;
}
<div id="ct" class="todo-box-scroll">
    @if (Model?.Count() > 0)
    {
        foreach (var dto in Model)
        {
            <partial name="TodoListItem" />
        }
    }
    else
    {
        <h2></h2>
    }
</div>